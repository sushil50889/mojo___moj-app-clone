import React from 'react'
import { View, Text } from 'react-native'

const ProfilePageUserBio = () => {
    return (
        <View style={{paddingHorizontal: 15, paddingVertical: 5}}>
            <Text style={{color: '#fff'}}>Enter Bio...</Text>
        </View>
    )
}

export default ProfilePageUserBio;
